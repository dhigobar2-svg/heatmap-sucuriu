{
  "name": "sesmt-app",
  "version": "1.0.0",
  "description": "SESMT APP - raiz que delega para o backend (usado pelo Railway).",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install"
  },
  "engines": {
    "node": ">=18"
  }
}
